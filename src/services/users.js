import{doc,getDoc} from "firebase/firestore";
import { db } from "../firebase";

export async function getUserLoggerd() {
/* const userId = getUserId();
if(userId){
    const ref = doc(db, 'users', userId);
    const dataUser = await getDoc(ref)
} */

}